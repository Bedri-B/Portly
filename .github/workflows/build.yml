name: Build & Release

on:
  push:
    tags: ["v*"]
    branches: [main]
    paths:
      - "app.py"
      - "build.py"
      - "requirements.txt"
      - "web/**"
      - ".github/workflows/**"
  pull_request:
    branches: [main]
    paths:
      - "app.py"
      - "build.py"
      - "requirements.txt"
      - "web/**"
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: portly-windows-amd64
            exe: portly.exe
            sep: ";"
          - os: ubuntu-latest
            artifact: portly-linux-amd64
            exe: portly
            sep: ":"
          - os: macos-latest
            artifact: portly-macos-arm64
            exe: portly
            sep: ":"

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Python deps
        run: pip install pyinstaller

      - name: Install web dependencies
        working-directory: web
        run: npm ci

      - name: Build web dashboard
        working-directory: web
        run: npm run build

      - name: Build executable
        run: >
          python -m PyInstaller
          --noconfirm --onefile --console
          --name portly
          --add-data "web/dist${{ matrix.sep }}web"
          --distpath dist
          app.py

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.exe }}
          retention-days: 30

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          cp artifacts/portly-windows-amd64/portly.exe release/portly-windows-amd64.exe
          cp artifacts/portly-linux-amd64/portly release/portly-linux-amd64
          cp artifacts/portly-macos-arm64/portly release/portly-macos-arm64
          chmod +x release/portly-linux-amd64 release/portly-macos-arm64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, '-rc') || contains(github.ref, '-beta') || contains(github.ref, '-alpha') }}
